export enum SubscriptionStatus {
  active = 'active',
  canceled = 'canceled',
  incomplete = 'incomplete',
  incomplete_expired = 'incomplete_expired',
  pastDue = 'past_due',
  paused = 'paused',
  trialing = 'trialing',
  unpaid = 'unpaid',
}
